# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageDeleteChecks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #pod 'DKImagePickerController', :git => 'https://github.com/zhangao0086/DKImagePickerController.git', :branch => 'Swift4'
  pod 'DKImagePickerController'

  # Pods for ImageDeleteChecks

  target 'ImageDeleteChecksTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageDeleteChecksUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
